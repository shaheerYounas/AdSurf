{
  "name": "amazon-ads-ai-control-center",
  "private": true,
  "version": "0.1.0",
  "description": "MVP foundation scaffold for the Amazon Ads AI Automation Control Center.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-ports.mjs all",
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "test:web": "npm --workspace apps/web run test",
    "dev:api": "node scripts/dev-ports.mjs api",
    "test:ports": "node --test tests/unit/dev-ports.test.mjs",
    "test:api": "python -m pytest tests/unit tests/integration",
    "test": "npm run test:ports && python -m pytest tests/unit tests/integration && npm --workspace apps/web run test",
    "lint": "npm --workspace apps/web run lint"
  },
  "engines": {
    "node": ">=20",
    "python": ">=3.12"
  }
}
